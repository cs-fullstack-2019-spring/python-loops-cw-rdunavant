def main():
    password = prompt("Ask user to enter password");
    password2 = prompt("Ask them to confirm it");
    if not (password == password2):
        while True
            Ask until (password == password2)
    or ("Enter 'Q")
